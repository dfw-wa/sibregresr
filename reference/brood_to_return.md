# Convert a brood table to a return table

Convert a brood table to a return table

## Usage

``` r
brood_to_return(bt)
```

## Arguments

- bt:

  brood table with column names: Stock, BroodYear, and for each age
  (e.g., Age3). Must be in that format.
